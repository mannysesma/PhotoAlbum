# PhotoAlbum on Replit

This is a Vite, React, and TypeScript web app.

## Run

Start the **Start application** workflow. It runs `npm run dev` and serves the app on port 5000.

## Verify

- Type-check: `npm run lint`
- Production build: `npm run build`

The app does not currently require environment secrets or external services.